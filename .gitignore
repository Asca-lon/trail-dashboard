# ── 비밀·환경 (절대 커밋 금지: 공공데이터포털·기상 API Hub 키) ──
.env
*.env
!.env.example

# ── Python (collector / processor / backend) ──
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# ── Node (frontend) ──
node_modules/
dist/
.vite/
*.log

# ── collector 원본 수집물 / 로컬 데이터 ──
data/
uploads/
results/
*.csv
*.parquet

# ── OS / 에디터 ──
.DS_Store
.idea/
.vscode/

# ── 로컬 데이터베이스 ──
*.db